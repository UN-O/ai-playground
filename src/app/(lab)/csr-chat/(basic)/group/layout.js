// Metadata
export const metadata = {
    title: "Client Side Rendering Group Chat",
    description: "Use `createStreamableValue` from AI SDK RSC and cut messages. (Senba)",
};



export default function Layout({ children }) {
	return children;
}