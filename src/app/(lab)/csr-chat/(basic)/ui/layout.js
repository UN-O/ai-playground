// Metadata
export const metadata = {
    title: "AI SDK UI: useChat",
    description: "Use `useChat` hook from AI SDK UI. (UNO)",
};



export default function Layout({ children }) {
	return children;
}