// Metadata
export const metadata = {
    title: "Draw Plot in Client Side",
    description: "Draw plot in client side from stream with a tool calling. (Senba)",
};



export default function Layout({ children }) {
	return children;
}