// Metadata
export const metadata = {
    title: "Step Answer Tool Result",
    description: "Show step answer result in client side from stream with a tool calling execute generate object. (UNO)",
};



export default function Layout({ children }) {
	return children;
}