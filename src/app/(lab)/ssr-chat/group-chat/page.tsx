import { GroupBlock } from "./group-block";

export const metadata = {
    title: "RSC Group Chat V1",
    description: "Chat in RSC return ReactNode Cut every sentences with [br] by Senba",
};

export default function GroupChat() {

    return (
        <div>
            <GroupBlock />
        </div>
    );
}
