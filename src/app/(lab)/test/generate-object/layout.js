// Metadata
export const metadata = {
    title: "AI SDK Core: generateText",
    description: "AI-Core generate text + bots chat to each other.",
};



export default function Layout({ children }) {
	return children;
}